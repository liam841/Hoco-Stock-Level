# Environment Variables Example
# Copy this file to .env and fill in your actual values
# DO NOT commit .env to git - it's in .gitignore

# Dropbox Access Token
# Get this from: https://www.dropbox.com/developers/apps
# This token should have 'files.content.write' permission
DROPBOX_ACCESS_TOKEN=your_dropbox_access_token_here

# Optional: Dropbox folder path where files will be uploaded
# Default: /TestStock/
DROPBOX_FOLDER_PATH=/TestStock/

# Optional: API endpoint URL (if using a separate backend)
# BACKEND_API_URL=https://your-api-domain.com

# Optional: Allowed file types (comma-separated)
# ALLOWED_FILE_TYPES=csv,txt

# Optional: Max file size in bytes (default: 10MB)
# MAX_FILE_SIZE=10485760

# Optional: Email notification settings (for Netlify Function)
# Using Gmail SMTP (recommended: use an app password, not your real password)

# Gmail SMTP server
# SMTP_HOST=smtp.gmail.com

# Gmail SMTP port (587 for STARTTLS)
# SMTP_PORT=587

# Gmail address used to send emails (must match the app password)
# SMTP_USER=your_address@gmail.com

# Gmail app password (NOT your normal Gmail password)
# SMTP_PASS=your_gmail_app_password_here

# Recipient email address for notifications (can be same as SMTP_USER)
# EMAIL_TO=you@example.com

# Sender email address shown in the email (usually same as SMTP_USER)
# EMAIL_FROM=your_address@gmail.com

